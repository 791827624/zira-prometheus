import Vue from 'vue'
import { Button, RadioGroup, RadioButton, Menu, MenuItem,
    MenuItemGroup, Input, Select, Option, Submenu, Tabs,
   TabPane ,Form,FormItem,Checkbox} from 'element-ui'


Vue.use(Button).use(RadioGroup).use(RadioButton).use(Menu)
    .use(MenuItem).use(MenuItemGroup).use(Input).use(Select)
    .use(Option).use(Submenu).use(Tabs).use(TabPane).use(Form).use(FormItem).use(Checkbox);
